import OptionsWrapper from './Options/OptionsWrapper';
import './Style.scss';

const Header = (props) => {
    return (
        <header>
            <OptionsWrapper/>
        </header>
    )
}

export default Header;